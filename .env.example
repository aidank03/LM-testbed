# The core workflow runs without an API key.
# To use the optional live OpenAI runner, set these in your shell.
# This project does not load .env automatically or store keys in outputs.
OPENAI_API_KEY=
LINER_LLM_MODEL=
